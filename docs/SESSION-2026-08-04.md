<!-- VERSÉ AU REPO le 2026-08-04 depuis le drop PO, TEL QUEL (« à verser au repo tel quel », visa PO
     au merge). Journal de la session claude.ai (décisions spec/architecture).

     ÉTAT D'EXÉCUTION CÔTÉ REPO (note d'édition, ne modifie pas le journal) :
     • Action 1 (step-0) : FAIT puis RÉVISÉ — R340–R377 = Wave 1, R378–R403 = Wave 2, R404 = FilterBar,
       PK R405+ (spec/mapping-session-repo.md §3/§4 ; CANON-MASTER régénéré).
     • Générateur Wave 1 + corpus GT : livrés sous tools/aml-gap/ (gen_aml_gap.py, 38 règles, 78 cas GT).
       La fusion avec le générateur PO tools/gen_aml_gap.py (vagues 1+2, 130 cas) est un lot ultérieur.
     • FilterBar (R404) + durcissement R-FB.4 : ce lot (composant React + migration écrans + tests).
     • Backend AML /v1/aml/*, worker aml-eval, Wave 2 : lots ultérieurs. -->

# SESSION 2026-08-04 — Filtres, gap analysis AML, waves 1+2

Journal de session Claude.ai (spec/architecture) → à verser au repo tel quel. Merge = visa PO.

## Décisions ratifiées
1. **FilterBar uniforme** (spec/SPEC-FILTERBAR.md) — un seul composant filtres pour toutes les listes : panneau rétractable + combobox + chips actifs supprimables + reset + compteur. Règle provisoire R-FB, scénarios FB-01…FB-07. 3 écrans migrés en démo (Règles AML, KYC, Clients), 7 restants planifiés (§3.2 de la spec).
2. **Gap analysis AML** (spec/GAP-ANALYSIS-AML.md) — 12 familles absentes/partielles, priorisation P1/P2/P3 beachhead GFI/EAM.
3. **Wave 1** (spec/SPEC-AML-GAP-WAVE1.md) — blocs 50-56, 38 règles R340-R377 provisoires : screening en flux, indices OBA-FINMA, vision groupe UBO, instruments PB, crypto/VASP, CFT, gouvernance du tuning. Architecture backend/front complète.
4. **Wave 2** (spec/SPEC-AML-GAP-WAVE2.md) — blocs 57-61, 26 règles R378-R403 : TBML, correspondent banking, prolifération, immobilier & art, analytique 2G (détecteurs statistiques exécutés dans le service Python CPSI — jamais réécrits en Nest).
5. **Dataset GT** (data/aml-gap-dataset-gt.json) — 130 cas plantés (66 TP / 64 FP), déterministes, clients du seed GWB. Sémantique : TP = alerte confirmée ; FP = alerte légitimement déclenchée puis écartée avec motif documenté (les deux DOIVENT déclencher — corpus de recall + calibrage de précision + matière Olivia).
6. **Source de vérité unique** (tools/gen_aml_gap.py + tools/wave2_rules.py) — toute modification de règle passe par le générateur qui ré-émet spec + dataset + bloc démo. Jamais d'édition manuelle des artefacts.
7. **Démo** (demo/olive-demo.html) — remplace l'ancienne : FilterBar sur 3 écrans, 64 règles gap intégrées au référentiel unifié (182 scénarios total), cas GT rendus TP/FP dans chaque carte, simulation opérationnelle. Suites e2e vertes (Playwright headless).

## Bugs corrigés (démo)
- **BUG-1 (racine)** : codes dupliqués `AML-10/11/12` dans AML_SCENARIOS (série CBK/WC vs série Retail) → clés React dupliquées → cartes orphelines au filtrage (18 rendues pour un compteur de 15). Fix : renumérotation `AML-CB-01`, `AML-CB-02`, `AML-WC-01` + invariant d'unicité au build du référentiel (suffixe déterministe + console.warn).
- **BUG-2** : casse « Correspondent banking » vs « Correspondent Banking » créait deux thèmes — aligné (attrapé par le test e2e de comptage).

## Écarts consignés (à reporter dans docs/ECARTS-FRONT.md)
- **E-FB-1** : la collision AML-10/11/12 peut exister dans le seed GWB backend — vérifier + test corpus d'unicité des codes de scénario.
- **E-FB-2** : AmlCatalogueScreen — `return;` orphelin dans le map des KPI : les 3 cartes KPI ne se rendent jamais (fix trivial, non appliqué ici).
- **E-FB-3** : barres « Déclenchements par catégorie » (Dashboard Compliance Center) non cliquables — décision PO ouverte : drill-down vers FilterBar pré-remplie, ou affichage seul.

## Actions Claude Code (ordre)
1. **Step-0 unique** pour R-FB + R340-R403 : numéros définitifs, table session→repo, régénération CANON-MASTER (R328).
2. Reporter E-FB-1/2/3 dans docs/ECARTS-FRONT.md ; corriger E-FB-2 ; vérifier E-FB-1 sur le seed GWB.
3. FilterBar : migrer les 7 écrans restants (SPEC-FILTERBAR §3.2), scénarios FB-01…FB-07 en Playwright (assertions par comptage DOM).
4. Waves 1+2 : Gherkin rouges bloc par bloc → implémentation backend (modèle Prisma, worker aml-eval, API /v1/aml/*, RBAC 4-yeux) → corpus GT seedé (130 cas, chacun DOIT déclencher) → front React.
5. Inscrire tous les paramètres tenant des 64 règles au registre R-Q.
6. Verser tools/gen_aml_gap.py au repo et brancher sa ré-émission dans le pipeline (toute évolution de règle = commit du générateur).

## Rappel sync
Après push : régénérer docs/, ré-uploader dans le panneau projet Claude.ai (catalogue, PROJECT-INDEX, README, cette session) — c'est la seule façon d'« enregistrer dans les sources du projet » côté Claude.ai (fichiers projet en lecture seule).

## Addendum — suite de session (même journée)
8. **FilterBar généralisée** : les 7 écrans restants du §3.2 migrés dans la démo — AML Investigation (statut+type), Tâches (statut allValue OPEN + priorité), Transactions Risk (risque), Registre LBA (risque + « revues en retard » en combobox binaire per R-FB.5), Account Review (statut réel OVERDUE/IN_PROGRESS/PENDING/COMPLETED + déclencheurs dynamiques), CRM/Journal (canal), Legal (statut contrat). Méthode non destructive : anciennes chips neutralisées par `false &&` (structure JS intacte, rollback = suppression du préfixe) — le front React fera la suppression propre. Suite e2e 7/7 PASS.
9. **E-FB-2 corrigé** : `return;` orphelin des KPI de AmlCatalogueScreen — les 3 cartes KPI se rendent désormais.
10. Écarts attrapés par les tests pendant la migration (consignés pour le front React) : le map statut d'Account Review est multi-lignes avec `AR_STATUS_CFG` (options réelles reprises telles quelles) ; l'écran CRM porte un second select « canal » dans le formulaire de saisie — les tests e2e doivent cibler les selects de filtre par une option unique (« Tous canaux »), pas par une valeur partagée.
