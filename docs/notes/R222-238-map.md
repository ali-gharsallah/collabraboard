# Vague R222–R238 — inventaire testable (P-L2-1)

> Constat lecture seule, aucun code modifié. Objectif : figer chaque identifiant R222→R238 par un
> cas de test minimal. ⚠ = garde **réglementaire** (sa violation serait un incident de conformité).

## Réconciliation de périmètre (le grep fait foi)

Le playbook cite `businesstrip, oprisk, custody, ta, crossborder, txflux, regwatch, formations, nba`.
En réalité, **le namespace R222–R238 vit dans DEUX modules seulement** :

- **R222–R230 → `apps/api/src/modules/businesstrip/businesstrip.module.ts`** (MOD-75 Business Trip).
- **R231–R238 → `apps/api/src/modules/formations/formations.module.ts`** (MOD-43 Formations & Certifications).
- **R237** est partagé : sa *sémantique* (certification résolue **à la date du voyage**) est appliquée
  **côté businesstrip** (`businesstrip.module.ts:71,82`), sa *config* côté `parametres`.

Les autres modules cités (`oprisk` R321–R323, `custody`/`ta` R301–R303, `crossborder`/`xb` R292–R293,
`txflux`/`fx`/`swift`/`txrisk` R297–R300, `regwatch` R309–R311, `nba`) portent **d'autres plages** —
hors R222–R238. Seules deux références *croisées* existent : `crossborder/xb.module.ts:10` (le country
manual R293 réutilise `tripCrossBorderReferentiel`, cf. R223/R229) et `common/visibility.ts:4`
(`teamScope` partagé, cf. R236).

**R227 — ABSENT.** `grep -rn "R227\b"` sur `apps/api/src`, `spec/`, `docs/` = **0 occurrence**. Signalé,
non inventé : aucun cas de test n'est produit pour R227 (le figer exigerait d'abord une définition canon).

Config tenant de la vague (référentiels gouvernés par date) : `apps/api/src/modules/parametres/parametres.service.ts:162-191`.

---

## Business Trip — R222→R230 (`businesstrip.module.ts`)

| R | ⚠ | Ancrage (fichier:ligne) | Comportement observable | Cas minimal (Given / When / Then) |
|---|---|---|---|---|
| **R222** | | `:48-51` créer=DRAFT · `:100` garde · ctrl `:187,:190` | **throw** : seul un `DRAFT` se soumet (sinon `BadRequest "R222"`) ; cycle événementiel `trip.submitted` | G un voyage `PENDING_APPROVAL` · W `soumettre` · T `BadRequest` (R222) |
| **R223** | ⚠ | `:34-44` `avisA` · `:90` avis attaché | **calcul** : avis cross-border (`AUTORISEE\|INTERDITE\|SOUMISE_A_LICENCE`) **attaché** au voyage — jamais une décision (R44) | G destination `INTERDITE` au référentiel · W `soumettre` · T advisory `INTERDITE` présent **et** le voyage passe quand même en `PENDING_APPROVAL` (aucun blocage auto) |
| **R224** | ⚠ | `:60-70` signal (`:69`) · `:115-116` garde viser | **signal + throw conditionnel** : `KYC_NOT_APPROVED` ; si sévérité `BLOQUANT_APPROBATION` → `viser` lève `TRIP_KYC_NOT_APPROVED` | G client visité sans KYC `VALIDATED` + `tripKycCheckSeverity=BLOQUANT_APPROBATION` · W `viser` · T `BadRequest TRIP_KYC_NOT_APPROVED` |
| **R225** | | `:86-89` visas (`:88` COMPLIANCE) · ctrl `:191` | **calcul** : visas = `tripApprovalMatrix` + visa `COMPLIANCE` si destination ∈ `tripJuridictionsRisque` (visa uniforme R15) | G destination listée à risque · W `soumettre` · T un `tripVisa` de rôle `COMPLIANCE` est créé en sus de la matrice |
| **R226** | | `:148-161` (`:159` `bloque:false`) · ctrl `:193` | **événement, jamais coercition (R39)** : contact reports manquants → `trip.contactreports.manquants`, retour `bloque:false` | G un client visité sans `crmContact` après `dateEnd` · W `mesurer` · T `manquants=[client]` et `bloque=false` |
| **R227** | | — | **ABSENT du code** (0 occurrence) | *aucun — signalé, non inventé* |
| **R228** | ⚠ | `:71-85` signal (`:84`) | **signal** : certification requise (`tripCertificationRequise`) non couverte **à `dateStart`** → `CERTIFICATION_EXPIRED_AT_TRIP_DATE` (sévérité tenant) | G certif requise en juridiction visitée, non couverte au `dateStart` · W `soumettre` · T signal `CERTIFICATION_EXPIRED_AT_TRIP_DATE` présent |
| **R229** | | `:163-174` (`:168-172`) · `:35` filtre version · ctrl `:189` | **calcul rejoué (grandfathering)** : `get(asOf)` recalcule les avis à la **version du référentiel en vigueur à `asOf`** | G référentiel v1 puis v2 (verdict changé) · W `get(asOf=dateEntreV1V2)` · T advisory rendu = celui de v1 |
| **R230** | | `:130-146` (`:135` garde, `:136-140` V2) | **throw + chaînage** : `reviser` exige `APPROVED` ; crée une V2 `DRAFT` (`revision+1`, `previousTripId`) repassée en `PENDING_APPROVAL`, V1 intacte | G voyage `APPROVED` · W `reviser` · T V2 créée `PENDING_APPROVAL`, V1 inchangée ; **et** réviser un non-`APPROVED` → `BadRequest R230` |

---

## Formations & Certifications — R231→R238 (`formations.module.ts`)

| R | ⚠ | Ancrage (fichier:ligne) | Comportement observable | Cas minimal (Given / When / Then) |
|---|---|---|---|---|
| **R231** | | `:33-37` `catalogue` · cfg `parametres:164-166` | **calcul** : le catalogue = `trainingCatalog` tenant, **aucun type en dur** | G `trainingCatalog` du tenant = [X] · W `catalogue` · T renvoie [X] (et non une liste codée en dur) |
| **R232** | ⚠ | `:62-79` (`:63` garde, `:71` événement) | **throw + événement** : compléter **exige** une attestation GED (`attestationDocId`) ; émet `training.completed` | G une assignation · W `completer` sans `attestationDocId` · T `BadRequest "R232"` |
| **R233** | | `:119-135` (`:129` événement, `:134`) | **événement, jamais coercition (R39)** : `tickRappels` émet `training.reminder` aux seuils `trainingReminderDays` | G certif expirant à J-30, seuils `[30,7]` · W `tickRappels(now)` · T un `training.reminder joursRestants=30`, aucun blocage |
| **R234** | ⚠ | `:100-108` (`:104`) · attestation `:70` | **append-only (R49)** : `certifier` **crée** une ligne (jamais d'écrasement) ; attestations idem | G une certif X émise · W `certifier` X à nouveau (renouvellement) · T 2 lignes `certification`, la 1re inchangée |
| **R235** | ⚠ | `:61-79` mode (`:73-74`) · `:81-98` (`:90` rôle, `:91` soi) | **paramètre tenant + throw four-eyes** : `AUTO`→`COMPLETED` ; `VALIDATED`→visa `PENDING`, validé par le bon rôle, **jamais soi-même (R13)** | G mode `VALIDATED`, complétion de l'utilisateur U · W U valide sa propre complétion · T `Forbidden TRAINING_SELF_VALIDATION_FORBIDDEN` (et mauvais rôle → `Forbidden R235`) |
| **R236** | | `:49-59` (`:55` `teamScope`) · cfg `parametres:172-173` | **calcul de périmètre** : visibilité soi / équipe (responsable) / tout (`trainingVisibiliteRoles`) | G rôle non habilité, assignations d'autrui · W `assignations` · T ne voit que son périmètre (soi + équipe) |
| **R237** | ⚠ | `businesstrip:71,82` · cfg `parametres:187-191` | **calcul à date** : certification requise résolue **à la date du voyage** (`obtenueLe ≤ dateStart < expireLe`), pas « maintenant » | G certif valide aujourd'hui mais **expirée au `dateStart`** · W `soumettre` · T signal certif non couverte (résolution à la date du voyage, R228/R237) |
| **R238** | | `:110-117` (`:115`) · ctrl `:147` | **calcul rejoué** : `certifications(asOf)` répond « qui était certifié X au JJ » depuis l'historique append-only | G historique de certifs · W `certifications(userId, asOf)` · T `certifie` = couverture à `asOf` (true/false) |

---

## Synthèse

- **Occurrences couvertes : 100 %** des R222–R238 présents au grep (R227 = 0, signalé absent).
- **Gardes ⚠ (7)** — priorité P-L2-2 : **R223** (avis ≠ décision, R44), **R224** (KYC bloquant, LBA),
  **R228** & **R237** (certification à la date du voyage), **R232** (attestation obligatoire),
  **R234** (append-only, R49), **R235** (four-eyes de validation, R13).
- **Non-⚠ (9)** — P-L2-3 : R222 (garde de cycle), R225 (matrice de visas), R226 & R233 (rappels
  informatifs, R39), R229 & R238 (rejeu/grandfathering), R230 (chaînage de révision), R231 (référentiel
  tenant), R236 (visibilité).
- Convention de test : **aucune spec de câblage** `businesstrip`/`formations` n'existe encore (à créer
  en P-L2-2/P-L2-3) — suivre les specs de câblage voisines (faux-Prisma en mémoire, `_db.events`,
  auteur = jeton, harnais `run-rule-tests.sh`). Sources Gherkin de référence : les en-têtes de module
  citent **BT-01..10** (businesstrip) et **FO-01..08** (formations) ; `spec/vague13-scenarios/FORMATIONS-MOD43.feature`
  existe pour les scénarios formation.
- Fichiers portant la vague (100 % couverts) : `businesstrip.module.ts`, `formations.module.ts`,
  `parametres/parametres.service.ts` (config), `crossborder/xb.module.ts` (réf. croisée R223/R229/R293),
  `common/visibility.ts` (réf. croisée R236).
