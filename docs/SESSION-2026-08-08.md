# SESSION 2026-08-08 — Bloc WD, React + NestJS (après fusion démo validée)

Journal d'exécution (prompt « SESSION — Bloc WD, React + NestJS »). Référence
comportementale : la démo fusionnée (`demo/wir-core.mjs`, session précédente, parcours
Playwright 11/11). Spec-first : les scénarios `spec/wd/WD-01..12.feature` (commit
précédent) branchés en Jest e2e ROUGES d'abord, puis l'implémentation.

## Livré

### Backend — module `workflow-designer` (R432–R438)

- `apps/api/src/modules/workflow-designer/wir.schema.ts` — WorkflowIR v1 (zod strict,
  versionné), `creerWir` (seuil Q-WD-3 : confidence < seuil ⇒ `aVerifier`, rien corrigé,
  R438) et `validerWir` R434 (NON_CONNEXE, INITIAL_ABSENT/MULTIPLE, TERMINAL_ABSENT,
  ARETE_ORPHELINE, ROLE_NON_MAPPE bloquant) — transcription fidèle de `demo/wir-core.mjs`.
- `workflow-designer.service.ts` — pipeline complet, AUCUNE mutation hors événement :
  - **import** : gardes Q-WD-1 (rôles CO/ADMIN) et Q-WD-4 (formats PNG/JPG/PDF, 10 Mo) →
    archivage GED réel (`GedIngestionService`, canal UPLOAD) + hash sha256 (R436) →
    `VisionExtractor` (interface E-WD-2 : API sous flag tenant `wdVisionApi` OU
    `StubVisionExtractor` déterministe hors-ligne) → WIR `DRAFT_AI` (R433) →
    `wd.wir.importe` au journal append-only ;
  - **état** : REJEU de `domain_events` (`wd.wir.importe` → `wd.wir.edite*` →
    `wd.wir.ratifie`), `?date=` reconstitue l'état à date (WD-10) ; `PUBLISHED` n'est
    JAMAIS décidé ici — lu du statut du brouillon dans le versioning existant (R436) ;
  - **édition** : `wd.wir.edite` (patch nodal), toute édition invalide le visa ;
  - **ratification** : refus `DRAFT_AI` (R433), refus importeur=ratifieur (R435/R13),
    rôle Q-WD-2 (CO_SR, ADMIN passe), anomalies R434 bloquantes → refus LISTÉ ; sinon
    brouillon créé via `WorkflowDefService.creerBrouillon` (circuit R29/R48 existant).
- Catalogue C6 : +3 types `wd.wir.importe/edite/ratifie` (`contracts/events-catalog.ts`).
- Registre R-Q : 6 entrées Q-WD-1..5 (`parametres.service.ts`), règles de rattachement
  `R432/R434/R435/R438`, défauts = arbitrages du prompt.
- Routes : `POST /v1/workflow-designer/import`, `GET :id (?date=)`, `PATCH :id/ir`,
  `POST :id/ratify` (snapshot API 390 → 394 routes, doc régénérée).

### Front — écran unique « Workflow Designer »

- `apps/web/src/features/workflow/DesignerWd.tsx`, même position que la démo (groupe
  Workflow, entre « Workflow » et « Workflow Instances ») : onglet **Designer (canvas)** =
  le `Builder` EXISTANT réutilisé (canvas contraint R304–R308/R437, pas de duplication) ;
  onglet **Import IA** = pipeline WIR servi par l'API — badge `DRAFT_AI — jamais activable
  (R433)` distinct et sans action de publication, zones illisibles AVEC coordonnées,
  nœuds sous seuil « À VÉRIFIER (R438) », anomalies R434 listées, ratification qui affiche
  le refus serveur TEL QUEL (FE-04).
- Router : entrée nav `builder` REMPLACÉE par `wfdesigner` (arbitrage AUDIT-NAV E-NAV /
  E-WD-4 fusion) ; le deep-link `builder` est conservé et rend le même écran.
- i18n : « Workflow Designer » ajouté EN/DE/IT + pack AR (`rapport-i18n` 0 écart) ;
  contenu de l'écran en `t()` (cliquet : 10 fichiers convertis, 0 texte en dur).

### Tests

- `wir.wiring.spec.ts` au harnais (6/6 — WD-01/03/04/05/06/12 unitaires purs).
- `test/e2e/workflow-designer.e2e-spec.ts` (6/6 contre Postgres réel) — import+GED+hash,
  DRAFT_AI jamais publiable, zones illisibles, formats refusés, refus même auteur PUIS
  visa croisé → brouillon `BROUILLON` du versioning existant, rejeu à date, rôle non
  mappé bloquant.
- `src/test/fe-wd-designer.test.tsx` (5/5 vitest + Testing Library) — l'écran contre un
  mini-backend qui rejoue le contrat : parcours import → édition → refus même auteur →
  visa croisé, zone illisible, anomalies affichées.
- `playwright/wd-parcours.spec.mjs` (3/3, vrai Chromium sur le bundle construit) —
  WD-03/12 (zone illisible + À VÉRIFIER), WD-08 (refus visa même auteur), parcours
  complet jusqu'au visa croisé. Voir écart n°2 ci-dessous sur le montage.

## Passe de vérification (mesurée, cette session)

| Porte | Résultat |
|---|---|
| Harnais règles (`run-rule-tests.sh`) | EXIT 0 — dont `### 6/6 specs WIR bloc WD verts ###` ; label CI 526 → **532** |
| e2e Postgres réel | **70 suites, 452/452** (69→70, 446→452) — label CI mis à jour |
| Lint + typecheck api (gate 1) | verts |
| Vitest web | **140/140** (135 + 5 FE-WD) |
| Build + budget bundle | 235.1 kB gz core (budget 240) |
| rapport-i18n / cliquet | 0 écart / 0 texte en dur |
| Playwright WD | 3/3 verts (fat-visuel : voir écart n°4) |
| Surface API (snapshot) | 394 routes, no-drift |
| CANON-MASTER | régénéré (les suites WD sont désormais mappées aux familles R432–R438) |

## Écarts & décisions consignés

1. **Habilitation du ratifieur (R173 × Q-WD-2)** — la ratification crée un brouillon via
   `WorkflowDefService`, dont la garde R173 lit `tenant.settings.workflowRoles` (défaut
   `["CO","ADMIN"]`). Un tenant dont le ratifieur est `CO_SR` (défaut Q-WD-2) doit donc
   AUSSI ajouter `CO_SR` à `workflowRoles` — paramétrage légitime, garde intacte (le
   e2e le documente). À afficher dans l'écran Paramétrage si un tenant s'y coince.
2. **Playwright WD : API rejouée au niveau réseau** — le parcours navigateur tourne sur
   le bundle CONSTRUIT (même harnais que la recette visuelle FAT, R332) avec
   `/v1/workflow-designer/*` rejoué par `page.route` au contrat exact du module — contrat
   lui-même prouvé contre Postgres par le Jest e2e. Le parcours pleine-stack (login JWT +
   NestJS + Postgres sous Playwright) n'est pas monté en CI : même doctrine que la
   décision PO 2026-07-29 (« API pour la porte CI + Playwright en job séparé »).
3. **E-WD-2 (licence vision) inchangé** — l'extraction reste derrière l'interface
   `VisionExtractor` ; en l'absence d'arbitrage licence, seul le stub déterministe est
   branché (flag tenant `wdVisionApi` prêt).
4. **Recette visuelle FAT rouge — PRÉEXISTANT, hors périmètre** — 5 scénarios de
   `fat-visuel.spec.mjs` cherchent des onglets directs alors que la nav est groupée
   (session antérieure). Non bloquant en CI (`continue-on-error`). Constat + correctif
   proposé : `docs/notes/fat-visuel-nav-groupee.md`. Les 3 scénarios WD, écrits pour la
   nav groupée, sont verts.
5. **E-WD-5 / E-WD-6** — déjà consignés dans `docs/ECARTS-FRONT.md` (session fusion démo).
