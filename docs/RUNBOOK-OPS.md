# O-Live — Runbook OPS (v1 · 19.07.2026)

Procédures d'exploitation. Principe : **rien ne se déclare vert sans run** ; chaque étape a sa
commande et sa preuve attendue.

---

## 1. Démarrage local (dev)

```bash
cd apps/api
cp .env.example .env      # DATABASE_URL · AUDIT_HMAC_SECRET · MFA_ENC_KEY · OIDC_* · OIDC_JWKS_URI
npm i
npm run prisma:migrate && npm run prisma:post   # migrations versionnées (lot 42) + triggers/RLS (post-deploy-v2)
npm run start:dev
```
Secrets **obligatoires au boot** (fail-fast volontaire) : `AUDIT_HMAC_SECRET` (audit sans valeur
probante sinon), `MFA_ENC_KEY` (chiffrement mfa_secret). L'appli refuse de démarrer sans.

## 2. Chaîne de vérification — l'ordre qui fait foi

| Étape | Commande | Preuve attendue |
|---|---|---|
| 1. Lint + types | `npm run lint && npm run typecheck` | 0 erreur — **bloquant en CI** (eslint.config.mjs racine + apps/api/tsconfig.json ajoutés au chantier B) |
| 2. Unitaires | — | **Couverte par `test:rules`** — pas de suite jest unitaire dans le dépôt. Step retiré du CI (`jest` sans config/spec ⇒ « No tests found ») ; à réintroduire **bloquant** le jour où un harnais jest unitaire existe. |
| 3. Règles + IAM + corpus session | `npm run test:rules` | **425 verts** (384 + **Couche Shariah IS-01..IS-15 / R207→R221 = 38**, lot 49) (339 + **Surveillance AML A-69..A-86 / R189→R206 = 45**, lot 48) (+ Surface consultation GED GS-01..05 lot 42 + Adaptateur WebDAV WD-01..05 lot 44 + Adaptateur Claude CL-01..05 R44/R138/R188 lot 45) (… + Licence vendor LC-01..05 R177→R179 + Port GED externe GX-01..05 R180→R182 + Capacité d'équipe WK-01..05 R183→R185 + CRM Relation CR-01..05 R186→R188 depuis le lot 40 ; cf. `docs/verify-run-2026-07-19.txt`) |
| 4. e2e Postgres réel | `npm run test:e2e:setup && npm run test:e2e` | **68/68** (kyc-rules 6 + … + V12 3 + V13 8 + V14 10, cf. docs/tests/) — exige le patch `kyc.controller` (guard `validate` retiré, sinon 403≠409) |
| 5. Moteurs Python | `python3 services/workflow-engine-py/run_tests.py` · `…/run_tests_sql.py` · `…/cpsi-server-py/run_tests.py` | 19/19 · SQL vert · **18/18** (⚠ faux-vert CPSI : ajouter `sys.exit(0 if total_ok==len(mods) else 1)` — la CI a une garde grep en attendant) |
| 6. Démo | `npm run test:smoke` (73 écrans) + onglet Screening → « 🧪 Preuves moteur » → Tout rejouer | 73/73 · 16/16 verts — **hors CI** : exige Playwright + navigateurs (hors scope CI actuel). Step retiré du workflow, même doctrine que test:unit ; à réintroduire bloquant quand l'environnement navigateurs sera provisionné. |

`verify:all` enchaîne 1→4. La CI (`.github/workflows/ci.yml`) rejoue le tout, `prisma:post`
inclus — les triggers R48 sont enfin exercés en continu. **Tous les steps CI sont bloquants**
(plus aucun advisory) : lint + typecheck, `prisma:post`, `test:rules` (425), e2e (68/68),
recette RLS, moteurs Python (19/19 · SQL 11/11 · CPSI 18/18). Hors CI : démo Playwright (étape 6).

> **CONFORMITÉ (lot 41, 21.07.2026)** — Lot d'arbitrage, **aucune règle nouvelle, harnais INCHANGÉ à 324**.
> Le modèle s'aligne sur les services ratifiés : `DomainEvent.at` devient un champ réel (les services
> l'écrivaient déjà), `DomainEvent.aggregateId` perd `@db.Uuid` (codes métier prouvés par les corpus,
> ex. R182/R183), `Document.expireAt` ajouté (active le signal R187 « pièce expirante », jusqu'ici
> dormant). Erratum E1 : `crm.service`/`crm.wiring.spec` alignés sur `Client.rmUserId` (comportement
> inchangé, CR 5/5). Non traités (backlog reconnu, lots dédiés) : surface GED `lister`/`lire`, typage `tx:any`.

> **CÂBLAGE (lot 43, 21.07.2026)** — Lot de **pur câblage DI, aucune règle, aucun test nouveau, harnais INCHANGÉ à 329**.
> `GedModule` (nouveau) câble les services GED par **`useFactory`** — les services à **ports optionnels** (`ports = {}` : ingestion, avancé, coffre, resolver) ne passent pas la résolution auto de Nest, la factory appelle le constructeur avec les ports vides (= comportement ratifié). `app.module` importe `GedModule`. **Boot RÉEL vérifié** : la DI résout (aucune erreur), `GET /v1/ged/documents` → 200 (filtré au registre : CO voit, INTRUS `[]` — R110), `GET /v1/ged/documents/<uuid-inexistant>` → 404. `ged.controller` : dépendance MORTE `GedService` retirée (aucun endpoint ne l'appelait) — aucun service modifié.

> **CÂBLAGE (lot 46, 21.07.2026)** — Trois portes HTTP minces (**délégation pure**, harnais INCHANGÉ à 339, aucun test nouveau) : `ParametresModule` (`GET /parametres/registre`, `GET`/`POST /parametres/valeur/:cle`), `CrmModule` (`timeline`/`gestes`/`pre-remplir`/`entretiens` — **sans port IA** : `pre-remplir` refuse R138), `WorkloadModule` (`equipes`/`mesures`/`surcharges`/`reassigner`/`points`/`snapshot-rh`) — importés par `app.module`, providers par `useFactory` (pattern lot 43). **Refus, motifs et traces vivent aux SERVICES**, pas aux controllers. **Boot RÉEL vérifié** : registre→200 (40 clés) · POST paramètre sans motif→400 (R7/R126) · timeline client inconnu→404 · pre-remplir sans clé→400 « port » (R138) · workload équipe par un non-responsable→403 (R183). **Aucun service modifié.**

> **BLOC 48 — Surveillance AML (22.07.2026, R189→R206, A-69..A-86)** — ⚠ **Provenance singulière** : le zip ne livrait NI service NI test ratifié (seulement un `.feature` + un catalogue de doc). Implémentation + tests **écrits depuis le Gherkin**, sur **exception ratifiée par Ali** à la doctrine « canon d'abord ». Ces tests valent ce que vaut le Gherkin — ils ne remplacent pas un corpus ratifié. Nouveau module `aml/` : `AmlScoringEngine` (18 détecteurs PURS R189→R206), `AmlService` (persistance append-only tenant-scopée, auteur = jeton, blocage niveau 1 sanctions/HRI/CRS), porte HTTP `POST /aml/evaluer` + `GET /aml/clients/:id/signaux`. Modèle `AmlSignal` (RLS FORCE + immuabilité). Seuils = **registre R-Q** (20 clés `aml*`, R7/R125) — l'onglet React « Paramétrages AML » n'est qu'une vue filtrée. Harnais **339 → 384** (+45). Boot RÉEL vérifié : sanctions→`bloque:true` signé jeton · structuring→signal N2 · GET signaux tenant-scopé. Recette RLS : `aml_signals` 0 ligne sans GUC, UPDATE refusé. Baseline migration régénérée (+`aml_signals`). Erratum **E3** séparé : dates relatives du spec vendor-license (base rétablie 339 avant travaux). *(NB : `apps/web` hors périmètre CI — l'onglet est vérifié par `vite build` local.)*

> **BLOC 49 — Couche Shariah (22.07.2026, R207→R221, IS-01..IS-15)** — ⚠ **Même provenance singulière que Bloc 48** : zip sans service ni test ratifié — implémentation + tests **écrits depuis le Gherkin**, sur exception ratifiée. **Adaptation minimale (option Ali)** : un seul modèle `IslamicSignal` (pas de modèle `Account`/enum ni tables Waqf/Mudaraba/Zakat — le prompt les demandait mais `Account` n'existe pas et `@db.String`/cuid/BigInt ne valident pas). Nouveau module `islamic/` : `IslamicScreeningEngine` (8 détecteurs purs R207/208/209/210/212/213/216/221 + 7 calculateurs Zakat/Qard/Mudaraba/Audit/Waqf/Takaful/Sukuk), `IslamicService` (persistance append-only tenant-scopée, auteur = jeton, **un seul blocage auto = maysir R209** ; **R216 = revue humaine, jamais d'auto-blocage** sur entité islamique caritative sanctionnée). Porte HTTP `POST /islamic/evaluer` + `/zakat` `/mudaraba` `/waqf/retrait` `/qard` `/takaful` `/sukuk/maturite` `/audit` + `GET /islamic/clients/:id/signaux`. Modèle `IslamicSignal` (RLS FORCE + immuabilité). 5 seuils au **registre R-Q** (`islamic*` : nisab, taux Zakat, secteurs haram, gharar, volatilité maysir). Onglet React « Finance Islamique » (Zakat + screening). Harnais **384 → 422** (+38, compte réel — le prompt annonçait « 105/534 », inexact). Boot RÉEL vérifié : maysir→`bloque:true` signé jeton · R216→`revueManuelle:true` non bloqué · Zakat 500k→12500 PENDING. Recette RLS : `islamic_signals` 0 ligne sans GUC, UPDATE refusé. Baseline régénérée (+`islamic_signals`). **Écarts prompt signalés** : base réelle 384 (pas 429), pas de modèle `Account`, dépendance « TenantAmlParameters » inexistante (Bloc 48 = registre).

> **BLOC 49b — Ledgers Shariah (22.07.2026, augmentation additive de Bloc 49)** — Sur re-envoi d'un Bloc 49 « schéma lourd » (déjà écarté), **option Ali = augmenter en additif**, PAS remplacer. On garde l'`IslamicSignal` minimal et on **persiste seulement les ledgers** des calculateurs, aux conventions du dépôt (**pas de modèle `Account` ni enum**, uuid/@db.Uuid, `Int` — pas de BigInt/cuid). 3 tables append-only tenant-scopées RLS FORCE : `zakat_calculations` (R211), `mudaraba_distributions` (R215), `waqf_distributions` (R218). `IslamicService.zakat/mudaraba/waqfRetrait` persistent désormais leur trace (auteur = jeton) + émettent l'événement ; `GET /islamic/clients/:id/zakat` relit l'historique. Harnais **422 → 425** (+3). Boot RÉEL vérifié : Zakat 500k→12500 persisté+relu signé jeton · Mudaraba 70/30→client 3600 persisté · Waqf retrait revenu-seul persisté, excès→400. Recette RLS : les 3 tables 0 ligne sans GUC, UPDATE refusé. Baseline régénérée (+3 tables). Qard/Takaful/Sukuk/Audit restent événement-seul (hors périmètre de l'augmentation).

> **RECETTE VAGUE 1 — FAT (22.07.2026)** — Plan de test + tests d'acceptation fonctionnelle métier (personas RM/CO/CF/MLRO/Auditeur) exécutés contre le **backend réel** : `apps/api/test/e2e/fat-vague1.e2e-spec.ts`. **10 FAT / 10 PASS (100 %)** : Clients (isolation tenant), KYC (four-eyes + golden record), AML (sanctions R192 bloque · structuring R189), Alertes (maysir R209 · caritative R216 revue humaine), **Rejeu à date R127** (paramètre : aujourd'hui=45, hier=30). e2e total **6 → 14** (kyc-rules 6 + FAT 8). Erratum **E4** : sous-requête `kyc-rules` scopée au tenant (le `code` KYC n'est unique que par tenant — correction stricte, sans impact fonctionnel). Documents : `docs/tests/` (PLAN-DE-TEST, FAT/, CAHIER-DE-TESTS, COUVERTURE-REGLES 10/10, PREUVES/, RAPPORT-RECETTE) + `docs/PROJECT-INDEX.md` (index maître).

> **VAGUE 2 — Surveillance & Dossiers (22.07.2026, spec-first)** — Deux écrans du cœur opérationnel, bâtis **Gherkin → e2e → code → vert** (`spec/vague2-scenarios/VAGUE2-ECRANS.feature`, `apps/api/test/e2e/fat-vague2.e2e-spec.ts`, **4 FAT / 4 PASS**). (1) **Instruction des dossiers de risque** (écran React `DossiersRisque.tsx`) : notes d'instruction **append-only** (R134), transitions gouvernées (R133/R136 ; un état terminal exige un motif — R7). Deux routes exposées **spec-first** au `risk-case.controller` — `POST`/`GET /v1/riskcases/:id/notes` — sur des méthodes de service **déjà ratifiées** (`noter`/`notes`), aucune règle nouvelle. (2) **Consultation des pièces (GED)** (écran React `GedPieces.tsx`) : liste filtrée au **rôle** relu à l'acte (R110), fiche = métadonnées + **empreinte** des versions, **jamais le contenu** (R145) — routes GED **inchangées**. **Aucun modèle Prisma nouveau** (`RiskCase`/`Document`/`DomainEvent` préexistent) ⇒ boucle RLS FORCE inchangée ; l'isolation tenant est prouvée à l'exécution (FAT-GED-02). e2e total **16 → 20** (kyc-rules 6 + FAT Vague 1 10 + FAT Vague 2 4). Harnais offline **inchangé à 425**. Front : 8 écrans (`vite build` vert, hors CI). Écart signalé : 12 erreurs `no-explicit-any` **préexistantes** dans les écrans Vague 1 (`AmlParametres`/`KycCreate`/`KycDetail`) — hors périmètre Vague 2, `apps/web` non gardé en CI. Documents : `docs/tests/FAT/FAT-VAGUE2.md`, CAHIER-DE-TESTS §Vague 2, COUVERTURE-REGLES (+R110/R134/R145), CERTIFICAT-ETAT (8 écrans), PREUVES/.

> **VAGUE 3 — Le cycle client de bout en bout (22.07.2026, spec-first)** — Six écrans pour qu'un dossier vive de l'entrée à la revue, sans trou (`spec/vague3-scenarios/VAGUE3-ECRANS.feature`, `apps/api/test/e2e/fat-vague3.e2e-spec.ts`, **7 FAT / 7 PASS**). (1) **Onboarding/aiguillage** (`Onboarding.tsx`) : entrée en relation + aiguillage SDD/CDD/EDD par le moteur de risque KYC (R117/R118), ouverture bloquée sans KYC VALIDATED (R119) — **routes existantes**. (2) **Account Review** (`AccountReview.tsx`) : **orchestration** de primitives ratifiées (re-screening R103 + visas KYC four-eyes) — **aucun agrégat « revue » inventé**. (3) **Screening** (`Screening.tsx`) : run (trace R103) + qualification motivée (R101/R7), escalade **proposée** jamais exécutée (R39/R44). (4) **Personnes/UBO** (`PersonnesLiees.tsx`) : rôles (R31) + relations bijectives (R34). (5) **Change of Circumstances** (`ChangementCirconstances.tsx`) : CoC propagé aux dossiers + re-screening **déclenché** sur champ d'identité (R30/R42). (6) **Dashboard exécutif** (`Dashboard.tsx`) : stock par état (onboardings/dossiers/hits), cloisonné RLS. **Portes HTTP nouvelles, spec-first, sur services ratifiés sans module** : `ScreeningModule` (`/v1/screening/run|hits|runs|hits/:id/qualify`), `PersonnesModule` (`/v1/personnes`, `/:id/roles`, `/relations`, `/:id/relations`, `/:id/coc`) + lecture `GET /v1/onboarding` (stock). **Aucun modèle Prisma nouveau** (tables `persons`/`person_roles`/`person_relations`/`screening*`/`onboardings` préexistent, déjà dans la boucle RLS FORCE) ⇒ harnais **inchangé à 425**, baseline en sync. e2e total **20 → 27**. Front : **14 écrans** (`vite build` vert, hors CI). Écarts signalés (non résolus par invention) : `PersonneLienService` (R152→R155) **dormant** — aucun modèle `Personne` au schéma ; le **% de détention** n'est pas un attribut ratifié — non fabriqué ; 12 `no-explicit-any` préexistants (écrans Vague 1). Documents : `docs/tests/FAT/FAT-VAGUE3.md`, CAHIER §Vague 3, COUVERTURE (+R30/R31/R34/R42/R100→R103/R117→R119), CERTIFICAT (14 écrans), PREUVES/.

> **VAGUE 4 — Écrans « plateforme » (22.07.2026, spec-first)** — Six écrans pour compléter l'offre CLM en INTÉGRANT (pas en refaisant) (`spec/vague4-scenarios/VAGUE4-ECRANS.feature`, `apps/api/test/e2e/fat-vague4.e2e-spec.ts`, **6 FAT / 6 PASS**). (1) **Transferts & ordres** (`TransfertsOrdres.tsx`) : portail transactionnel — verdict tracé garde par garde, file de revue habilitée (R143), décision motivée (R7), statut client SANS motif AML (art. 10a, R132). (2) **Settlement** (`Settlement.tsx`) : le core est un **PORT** (R167→R169) — état sync en lecture seule ; sans connecteur, import **refusé** (R114/R167), jamais de simulacre. (3) **Screening avancé** (`ScreeningAvance.tsx`) : adverse media / listes complémentaires = **paramètre d'entrée** du moteur ratifié (R100→R103), pas un moteur séparé. (4) **Reporting MROS** (`ReportingMros.tsx`) : registre opposable — décision figée + empreinte `dossierSha256` (R130), relecture à l'identique, art. 10a (R132). (5) **GED / coffre** (`GedCoffre.tsx`) : versions = preuve d'intégrité, jamais le contenu (R110/R145). (6) **Registre LBA** (`RegistreLBA.tsx`) : piste d'audit **agrégée** (MROS + verdicts + runs), cloisonnée RLS. **Portes HTTP nouvelles, spec-first, sur services ratifiés sans module** : `TransactionsModule` (`/v1/transactions/evaluer|revue|:id/decider|:id/statut-client`, garde `comportement` R142 câblée), `MrosModule` (`/v1/mros` + `decider|:id|:id/notification|:id/gel`), `CorebankingModule` (`/v1/corebanking/etat|importer`) + lecture `mros.lister`. **Aucun modèle Prisma nouveau** (tables `tx_verdicts`/`mros_communications`/`core_*` préexistent, déjà RLS FORCE) ⇒ harnais **inchangé à 425**, baseline en sync. e2e total **27 → 33**. Front : **20 écrans** (`vite build` vert, hors CI).
> **Correctif d'infrastructure (fond, ratifié par la nécessité)** : `PrismaService` n'avait PAS de `onModuleDestroy` → chaque `app.close()` laissait ses connexions ouvertes ; en e2e (5 suites × ≈18 clients — un par module câblant sa PrismaService), elles s'accumulaient jusqu'à saturer Postgres (« too many clients »). Ajout de `onModuleDestroy(){ $disconnect() }` (libère au shutdown) + `connection_limit=3` sur la `DATABASE_URL` de test/CI (plafond par client — 1 seul saturait, starvation des transactions interactives en CI). **Dette signalée** : un `PrismaModule` @Global (client unique) reste le correctif d'architecture — lot dédié.
> **Écart signalé (fiche GED)** : la consultation GED (`ged-consultation.fiche`) mappe `v.no`/`v.empreinte`/`v.creeAt`, mais le modèle Prisma expose `numero`/`sha256`/`deposeAt` — le **fake** du corpus GS-04 utilise `no`/`empreinte` (harnais vert) alors que le **vrai** endpoint renvoie des métadonnées de version `undefined`. Correctif = aligner service + fake ratifié → hors périmètre Vague 4 (touche le canon GS). L'écran GED/coffre liste les versions et signale l'empreinte non restituée le cas échéant. Documents : `docs/tests/FAT/FAT-VAGUE4.md`, CAHIER §Vague 4, COUVERTURE (+R110/R130/R132/R140→R143/R167→R169), CERTIFICAT (20 écrans), PREUVES/.

> **VAGUE 5 — Rattrapage maquette : CRM & Workflow (26.07.2026, spec-first)** — Quatre écrans qui rapprochent le React de la maquette `olive-demo.html`, **là où le canon est ratifié** (`spec/vague5-scenarios/VAGUE5-ECRANS.feature`, `apps/api/test/e2e/fat-vague5.e2e-spec.ts`, **4 FAT / 4 PASS**). (1) **CRM Banque** (`CrmBanque.tsx`) : timeline projetée (R186) + prochains gestes proposés (R187) — routes CRM **déjà câblées**. (2) **Contact Reports** (`ContactReports.tsx`) : compte rendu tracé (R188) ; pré-remplissage IA **refusé sans port** (R138). (3) **Workflow Designer/Rules** (`WorkflowDesigner.tsx`) : définition **versionnée & gouvernée** — publier = version datée + immuable (R171), habilitée + motivée (R173/R7), résolution datée = grandfathering (R172). Porte **nouvelle** `WorkflowModule` sur `WorkflowDefService` ratifié. (4) **Corroboration KYC** (`CorroborationKyc.tsx`) : divergence d'identité → dossier Central File + tâche, **aucune donnée modifiée** (R36) — route ajoutée à `PersonnesModule` (`signalerDivergence`). **Aucun modèle Prisma nouveau** (`workflow_defs` préexiste, déjà RLS FORCE) ⇒ harnais **inchangé à 425**, baseline en sync. e2e total **33 → 37**. Front : **24 écrans** (`vite build` vert, hors CI). **Zéro invention** : tous les backends étaient ratifiés. Documents : `docs/tests/FAT/FAT-VAGUE5.md`, CAHIER §Vague 5, COUVERTURE (+R36/R171→R173/R186→R188), CERTIFICAT (24 écrans), `docs/DECALAGE-FRONT-DEMO.md` (gap maquette), PREUVES/.

> **VAGUE 6 — Paramétrage & Gouvernance de la configuration (26.07.2026, spec-first)** — Deux écrans sur le **registre R-Q ratifié** (R125→R128) — `spec/vague6-scenarios/VAGUE6-ECRANS.feature`, `apps/api/test/e2e/fat-vague6.e2e-spec.ts`, **2 FAT / 2 PASS**. (1) **Registre de paramétrage** (`ParametrageRegistre.tsx`) : le registre se génère du canon (R125) ; écrire = acte **typé, motivé (R7/R126), daté, jamais rétroactif** ; valeur d'alors relue à une date (R127). (2) **Config à date & Go-live** (`ConfigGolive.tsx`) : config **complète reconstruite** à une date (R127) + **activation gouvernée** (R128 : signature obligatoire, clé requise manquante **nommée**). Deux routes ajoutées à `ParametresController` (`GET /v1/parametres/config`, `POST /v1/parametres/activer`) sur des méthodes de service **déjà ratifiées** (`configALaDate`, `activer`). e2e total **37 → 39**. Front : **26 écrans** (`vite build` vert, hors CI).
> **CONFORMITÉ (schéma ↔ canon)** : `ParametresService.activer` (R128) écrivait `tenant.statut`/`rqSignePar`/`rqSigneAt` — **absents du modèle `Tenant`** (le fake du corpus les acceptait, le vrai endpoint plantait « Unknown argument statut »). Ajout des 3 colonnes nullables (`statut` défaut `BROUILLON`), **baseline régénérée**, `prisma validate` OK, harnais **inchangé à 425** (fakes), e2e **39/39**. `tenants` n'est pas dans la boucle RLS FORCE (table racine) — colonnes nullables sûres, aucun post-deploy modifié. **DRY-RUN sandboxes différés** : les bacs à sable de simulation (R93→R99 : abaisser un seuil → alertes nommées, stress test, tension) n'ont **pas** de service Nest ratifié — construits uniquement dans la maquette ; à ouvrir sur canon. Documents : `docs/tests/FAT/FAT-VAGUE6.md`, CAHIER §Vague 6, COUVERTURE (+R125→R128), CERTIFICAT (26 écrans), PREUVES/.

> **VAGUE 7 — PMS : Mandats, Adéquation & Breaches (26.07.2026, spec-first)** — Un écran « plateforme » de plus, **INTÉGRER pas refaire** : la couche COMPLIANCE sur les positions (importées d'un core), jamais un moteur de portefeuille (`spec/vague7-scenarios/VAGUE7-ECRANS.feature`, `apps/api/test/e2e/fat-vague7.e2e-spec.ts`, **2 FAT / 2 PASS**). **PMS** (`PmsMandats.tsx`) : adéquation LSFin bornée par le riskLevel CLIENT (R107 : mandat HIGH sur client LOW refusé, alerte sans rétrogradation) · valorisation → **drift CONSTATÉ** (R105 : breach OUVERT inscrit, **positions intactes**, jamais de rééquilibrage auto — R44) · **pre-trade bloquant** (R106 : exclusions sectorielles + concentration) · **registre de breaches** (R108/R7 : l'échéance escalade sans liquider, clôture motivée). Porte **nouvelle** `PmsModule` sur `PmsService` ratifié + 2 lectures additives (`mandats`/`breaches`). **Aucun modèle Prisma nouveau** (`mandates`/`positions`/`pms_breaches` préexistent, déjà RLS FORCE) ⇒ harnais **inchangé à 425**, baseline en sync. e2e total **39 → 41**. Front : **27 écrans** (`vite build` vert, hors CI). **Zéro invention** : PMS déjà ratifié R105→R108. Documents : `docs/tests/FAT/FAT-VAGUE7.md`, CAHIER §Vague 7, COUVERTURE (+R105→R108), CERTIFICAT (27 écrans), `docs/DECALAGE-FRONT-DEMO.md`, PREUVES/.

> **VAGUE 8 — Référentiel AML : scénarios & seuils (26.07.2026, spec-first)** — L'écran « Référentiel AML » de la maquette (`amlcat`), sur le canon AML **déjà ratifié** (R189→R206, A-69..A-86) — `spec/vague8-scenarios/VAGUE8-ECRANS.feature`, `apps/api/test/e2e/fat-vague8.e2e-spec.ts`, **2 FAT / 2 PASS**. **Référentiel AML** (`ReferentielAml.tsx`) : projection LISIBLE des **18 scénarios** de surveillance (code, type, niveau, libellé) + leurs **seuils EFFECTIFS**, pilotés par le registre R-Q (clés `aml*`). Un `REFERENTIEL_AML` (index factuel des 18 détecteurs) est ajouté à `aml-scoring.engine.ts` ; `AmlService.referentiel` rend `{ scenarios, seuils: paramsDepuisSettings(tenant.settings) }` via **`GET /v1/aml/referentiel`**. **Tie-in gouvernance** (FAT-AMLCAT-02) : un seuil changé au **registre** (Vague 6, R126) se reflète **à chaud** dans le référentiel — les règles ne sont jamais en dur. **Aucun modèle Prisma nouveau**, harnais **inchangé à 425** (AML 45/45), baseline en sync. e2e total **41 → 43**. Front : **28 écrans** (`vite build` vert). **Zéro invention** : catalogue = projection du canon. Documents : `docs/tests/FAT/FAT-VAGUE8.md`, CAHIER §Vague 8, COUVERTURE (+R189→R206 référentiel), CERTIFICAT (28 écrans), PREUVES/.
>
> **VAGUE 9 — Bac à sable AML : dry-run d'un seuil (26.07.2026, spec-first)** — L'écran « Bac à sable AML » de la maquette (`sbaml`) — *voir avant d'écrire* — sur le canon **désormais confirmé ratifié** (R94, scénario **B-02**, `spec/catalogue-amendements-R89-R99-ratifies.md`) : `spec/vague9-scenarios/VAGUE9-ECRANS.feature`, `apps/api/test/e2e/fat-vague9.e2e-spec.ts`, **2 FAT / 2 PASS**. **Sandbox AML** (`SandboxAml.tsx`) : on rejoue le moteur **PUR** ratifié `evaluer(contexte, params)` avec les seuils **actuels** (`paramsDepuisSettings(tenant.settings)`) puis **simulés** (`{...settings, [cle]: valeur}`), et l'on rend l'impact **NOMINATIF** (alertes avant/après, nouvelles/disparues, chaque nouvelle **nommée** : client, fait, règle franchie). Porte **nouvelle** `POST /v1/aml/sandbox` (`AmlService.sandbox`). **`ecriture=false` prouvée** : lecture seule du tenant + moteur pur, **0 signal en base** après simulation (R70/R94 : ni signal, ni tâche, ni case). La clé simulée doit être un paramètre **AML gouverné du registre** (préfixe `aml`, R125) — sinon `400`. **Propose ≠ applique** (FAT-SBAML-02, R96) : la simulation ne matérialise rien ; **appliquer** passe par la porte d'écriture existante du registre R-Q (`POST /v1/parametres/valeur/:cle`, motivé R126, daté R29, journalisé). **Aucun modèle Prisma nouveau**, moteur AML **inchangé**, harnais **inchangé à 425** (AML 45/45), baseline en sync. e2e total **43 → 45**. Front : **29 écrans** (`vite build` vert). **Zéro invention** : dry-run = R94 ratifié rejoué sur le moteur ratifié. Ceci **lève** le report noté en Vague 6 (« DRY-RUN sandboxes différés ») pour le bac AML. Documents : `docs/tests/FAT/FAT-VAGUE9.md`, CAHIER §Vague 9, COUVERTURE (+R70/R94/R96), CERTIFICAT (29 écrans), PREUVES/.
>
> **VAGUE 10 — Front-câblage v2, phase 1 : FE-CORE + Ports + NBA (26.07.2026)** — Première passe de `SPEC-FRONT-CÂBLAGE v2`, **après confrontation au réel** (`docs/ECARTS-FRONT.md`) et **décisions d'Ali actées** : portes minces là où un service ratifié existe · JWT par défaut · adoption **incrémentale** · préfixe `/v1`. **Constat cardinal** : sur les 4 blocs dits « constructibles », **Workflow Instances** et **Tâches** n'ont **aucun service ratifié** (le module `workflow` porte les *définitions* R171-173, pas les instances ; pas de backlog de tâches) → **GELÉS, non codés** (signalés, jamais inventés). Livré : (1) **FE-CORE** (`src/lib/api.ts`) étendu en incrémental — `apiPost` (erreurs normalisées `{code,status,message}` affichées telles quelles), rejeu `asOf` (R48/R49), `OLIVE_AUTH_MODE` (JWT défaut ; headers-mode câblé **inerte**), hook `useApiOrSeed` ; **Vitest** introduit (`apps/web`, 5 tests FE-01..04). (2) **Ports** (`Ports.tsx` + **porte nouvelle** `PortsModule` : `GET /v1/ports`, `GET|POST /v1/ports/:id/health`) — projection **lecture seule** des ports **ratifiés** (core banking R167, IA R163, coffre R180), statut = présence de la config au registre R-Q, **jamais le secret** (« pas de secret = refus gracieux ») ; fx/custody/mobile non ratifiés → non listés. (3) **NBA** (`NextBestAction.tsx`) — **lecture** des gestes R187 (`crm/clients/:id/gestes`) avec cadre R44 ; la **route de décision** n'étant pas ratifiée, les actions sont présentées **désactivées** (attente canon). **Aucun modèle Prisma nouveau**, harnais **inchangé à 425**, baseline en sync. e2e **45 → 47** (FAT-PORT-01/02) ; front FE-CORE **5/5** (Vitest). Front : **31 écrans** (`vite build` vert). **Gelé (attente « OK pour R222..R238 »)** : Business Trip (MOD-75) & Formations (MOD-43) — Gherkin seul dans `spec/proposed-R222-R238/`. Documents : `docs/ECARTS-FRONT.md`, `docs/tests/FAT/FAT-VAGUE10.md`, CAHIER §Vague 10, COUVERTURE (+ports/FE-CORE), CERTIFICAT (31 écrans), PROJECT-INDEX, DECALAGE, CI 47.
>
> **VAGUE 11 — Amendement A1 : harnais front + FE-05 (26.07.2026)** — `SPEC-FRONT-CÂBLAGE v2 · A1` ratifie les 4 arbitrages (D1 portes minces / D2 JWT défaut / D3 incrémental / D4 `/v1`) et fournit **FE-05** (écran sans service ratifié → **seed lecture seule**, aucun endpoint fictif). Application, **sans toucher le backend** : (1) **Harnais D3** — `theme/tokens.ts` (palette O-Live) ; **Vitest + Testing Library + MSW** installés ; setup jest-dom. (2) **api.ts** — `authMode()` (défaut `jwt`), `isDevAuthMode()` (bandeau « Mode dev — auth simulée » quand `OLIVE_AUTH_MODE='headers'`) ; préfixe `/v1` (base = racine, un seul point de concat — FE-06). (3) **FE-05 écrans** — `WorkflowInstances.tsx` (le canon n'expose que les *définitions* R171-173, pas d'instances) et `Tasks.tsx` (pas de service backlog list/complete ; seul `workload.reassigner` existe) : seed lecture seule + bandeau, bouton « Compléter » **absent** (capacité non ratifiée). **Aucune porte inventée.** Tests front **5 → 11** (Vitest : FE-01..06 lib + FE-05/10/40 composants RTL+MSW). **Backend intact** : e2e **47/47**, harnais **425/425**, baseline en sync (aucun fichier `apps/api` touché). Front : **33 écrans** (`vite build` vert). Boy-scout tracé : `docs/MIGRATION-FRONT.md` (0 écran existant migré). **Écart consigné (A1, pas de résolution silencieuse)** : services WFI / Tasks-backlog / décision-NBA absents du canon → relèvent d'un futur **amendement A2**. Documents : `docs/ECARTS-FRONT.md` §5, `docs/MIGRATION-FRONT.md`, CAHIER §Vague 11, CERTIFICAT (33 écrans), PROJECT-INDEX, DECALAGE.
>
> **VAGUE 12 — Workflow Instances réel (FE-WFI) (27.07.2026)** — À la demande d'Ali (« un écran complet lié au back end qui gère les instances/visa »), l'écran Workflow Instances passe de **FE-05 seed** à **réellement câblé** — **sans inventer de canon** : l'instance projette le **workflow gouverné ratifié** = le **dossier KYC** (`kyc-workflow.chaine`, R171-173 le résolvent). Porte **nouvelle** `WorkflowInstancesModule` (lecture seule, projection) : `GET /v1/workflow-instances` (liste), `GET /:id` (steps = sections + visas R15), `GET /:id/events` (timeline append-only par `aggregateId`, ordre serveur — FE-20). Sur `KycFile`+`KycVisa`+`DomainEvent` ratifiés ; **aucun modèle Prisma nouveau, aucune règle nouvelle** (PT-01 : relaie/projette, ne décide pas ; métadonnées seulement, jamais le contenu des questions R110). Front : `WorkflowInstances.tsx` (liste → détail : workflow horizontal, panneau visas via `<VisaBadge>` **composant unique** R15, timeline), `asOf` → lecture seule ; `apps/web/src/components/VisaBadge.tsx`. Recette : **3 FAT** (FAT-WFI-01/02/03) + composant FE-WFI (Vitest+RTL+MSW). e2e **47 → 50**, harnais **inchangé à 425**, baseline en sync. Front : **33 écrans** (Workflow Instances désormais réel ; **Tâches** reste FE-05 — pas de service backlog ratifié). Documents : `docs/tests/FAT/FAT-VAGUE12.md`, ECARTS-FRONT §5 (WFI résolu), CAHIER §Vague 12, COUVERTURE, PLAN, CERTIFICAT (33 écrans, ~123 routes), PROJECT-INDEX, DECALAGE, CI 50.
>
> **VAGUE 13 — MOD-43 Formations & Certifications (R231→R238) (27.07.2026, spec-first)** — Sur ratification **« OK pour R222..R238 »** d'Ali, le premier des deux domaines débloqués. Bâti Gherkin (FO-01..08) → e2e → code → vert (`spec/vague13-scenarios/FORMATIONS-MOD43.feature`, `apps/api/test/e2e/fat-vague13.e2e-spec.ts`, **8 FAT / 8 PASS**). **Module nouveau `FormationsModule`** : catalogue 100% tenant (R231), assignations + complétion **événementielle** avec attestation GED (R232), validation par **visa uniforme R15** en mode VALIDATED (R235) avec **exclusion R13** (`TRAINING_SELF_VALIDATION_FORBIDDEN`), rappels J-x **informatifs** (R233/R39), **rejeu certifiant** à date (R238), visibilité par profil (R236). **3 modèles Prisma nouveaux** : `TrainingAssignment`, `Certification` & `TrainingAttestation` (**append-only** R234 — triggers `audit_immutable`) ; **3 tables RLS FORCE tenant** ajoutées au post-deploy ; **baseline régénérée** (`prisma validate` OK, RLS recette : olive_app 0 ligne sans GUC). Registre R-Q enrichi (R231/R233/R235/R236). Front `Formations.tsx` (catalogue, dossiers par profil, dépôt attestation → GED via backend, visa, rejeu certifiant) + composant FE-FORM (Vitest+RTL+MSW). **Harnais offline inchangé à 425** (fakes) ; e2e **50 → 58** ; Vitest **11 → 12**. Front : **34 écrans**. Note d'ordonnancement : MOD-43 bâti **avant** MOD-75 car BT-08/R237 lit la certification voyageur **depuis MOD-43**. Documents : `docs/tests/FAT/FAT-VAGUE13.md`, `spec/vague13-scenarios/`, CAHIER §Vague 13, COUVERTURE (+R231→R238), PLAN, CERTIFICAT (34 écrans, ~131 routes), PROJECT-INDEX, DECALAGE, CI 58.
>
> **VAGUE 14 — MOD-75 Business Trip (R222→R230) (27.07.2026, spec-first)** — Second domaine ratifié « OK pour R222..R238 » ; **R222→R238 désormais intégralement implémenté**. Gherkin (BT-01..10) → e2e → code → vert (`spec/vague14-scenarios/BUSINESS-TRIP-MOD75.feature`, `apps/api/test/e2e/fat-vague14.e2e-spec.ts`, **10 FAT / 10 PASS**). **Module nouveau `BusinessTripModule`** : cycle **événementiel** (R222, `TRIP_SUBMITTED`) ; pré-contrôle **cross-border** = un AVIS attaché qui **ne décide pas** (R223) ; signaux **KYC** (R224 : INFORMATIF vs BLOQUANT → `TRIP_KYC_NOT_APPROVED`) et **certification** (R228/R237, résolue **depuis MOD-43** à la DATE DU VOYAGE → `CERTIFICATION_EXPIRED_AT_TRIP_DATE`) ; approbation = **visa uniforme R15** (R225 : matrice + COMPLIANCE si destination à risque), le voyageur ne vise jamais son voyage (R13 → `TRIP_SELF_APPROVAL_FORBIDDEN`) ; contact reports **mesurés** jamais coercés (R226/R39) ; **rejeu grandfathering** du référentiel (R229, `?asOf=`) ; **révision chaînée** après approbation (R230, V2 PENDING_APPROVAL, V1 intacte). **2 modèles Prisma nouveaux** : `Trip` (avis/signaux/visas projetés à la soumission) & `TripVisa` (R15) ; **RLS FORCE tenant** ; **baseline régénérée** (recette RLS : olive_app 0 ligne sans GUC). Registre R-Q enrichi (R223→R228). Front `BusinessTrip.tsx` (liste/création/soumission ; détail avis colorés par verdict + signaux + visas via `<VisaBadge>`) + composant FE-TRIP. **Harnais offline inchangé à 425** ; e2e **58 → 68** ; Vitest **12 → 13**. Front : **35 écrans**. `spec/proposed-R222-R238/` n'a plus de règle en attente. Documents : `docs/tests/FAT/FAT-VAGUE14.md`, `spec/vague14-scenarios/`, CAHIER §Vague 14, COUVERTURE (+R222→R230), PLAN, CERTIFICAT (35 écrans, ~140 routes), PROJECT-INDEX, DECALAGE, CI 68.

> **RÉSOLU (chantier « alignement Document », 20.07.2026)** : le modèle `Document`/`DocumentVersion`
> est désormais aligné sur le contrat GED R109→R116 + R137→R139 — champ Prisma = contrat
> (`statut`, `nom`) avec `@map` conservant les colonnes v0.2 (migration douce), `clientId`/`retentionUntil`
> nullable, ajout `ingereAt`/`inboxSignale`/`ocrDerives`, champs v0.2 hors contrat (`s3Key`,`code`,`lang`,
> `sizeBytes`,`uploadedBy`) conservés en nullable. **Preuve de fin : le pont `as any` du bloc 24 est
> retiré et le typecheck reste vert.** Migration des données `A_VALIDER → ACTIF` (mapping ratifié Ali,
> idempotente : `prisma/data-migration-document-statut.sql`).

## 3. Déploiement — ordre impératif

1. **Migrations** : `prisma migrate deploy` puis `prisma:post` (**post-deploy-v2.sql** : immuabilité
   append-only sur les 5 journaux, garde outbox « seul published_at mutable », RLS **FORCE** + policies).
2. **Bascule RLS en 2 temps, sans interruption** :
   T1 — exécuter le SQL (l'appli, en propriétaire, bypasse encore : rien ne change) ;
   T2 — basculer `DATABASE_URL` sur le rôle **olive_app** (non-propriétaire) + activer
   `PrismaService.forTenant` (SET LOCAL `app.tenant_id`). Test de recette : en `olive_app` sans
   `set_config`, `SELECT * FROM clients` → **0 ligne**.
3. **Rotation de clés JWT** : `KeyStore.rotate()` — période de grâce 3 clés, les sessions en cours
   survivent (résolution par kid via `/.well-known/jwks.json`).
4. **SSO** : renseigner `OIDC_JWKS_URI` — `verifyIdToken` est branché sur le JWKS réel de l'IdP
   depuis le 19.07 (JV-01..07) ; sans la variable, le login OIDC refuse proprement.
5. **Adaptateurs réels (lot 43)** : l'injection des ports de production — coffre S3 suisse
   (Exoscale SOS), GED externe (CMIS/WebDAV) — se fait en **remplaçant les factories du
   `GedModule`** (`useFactory`), **jamais** les services (dont le comportement est ratifié et testé).
6. **Bascule WebDAV (lot 44)** : (a) renseigner `WEBDAV_*` ; (b) remplacer la factory
   `StorageResolverService` du `GedModule` par `new StorageResolverService(p, { GED_EXTERNE:
   new WebDavStorageAdapter({...}) })` ; (c) basculer le registre `docStorage` du tenant sur
   `GED_EXTERNE` — **acte motivé** (R7/R181, jamais rétroactif) ; (d) recette : un **dépôt →
   relecture sonde** (le coffre recalcule l'empreinte R145 ; falsification côté GED → refus
   explicite, panne → refus explicite). Le mot de passe ne voyage qu'en en-tête, jamais journalisé.
7. **Port IA réel — adaptateur Claude (lot 45)** : **sans `ANTHROPIC_API_KEY`, PAS de port** —
   la factory `claudeIaAdapter()` refuse explicitement (R138) ; l'appel eager du câblage est **gardé**
   (`ia: process.env.ANTHROPIC_API_KEY ? claudeIaAdapter() : undefined`), donc l'app **démarre** et les
   fonctions assistées (pré-revue R44, complétion, pré-remplissage CRM R188) **refusent proprement à
   l'appel**. Avec clé : injection aux modules par factories (pattern lot 43) ; le secret ne voyage qu'en
   en-tête `x-api-key`, jamais dans l'URL ni un message d'erreur (masqué).

## 4. Incidents — chemins courts

| Symptôme | Cause probable | Geste |
|---|---|---|
| Boot refuse : `AUDIT_HMAC_SECRET manquant` / `MFA_ENC_KEY` | secret absent | poser la variable — ne PAS contourner le fail-fast |
| e2e `validate` rend 403 au lieu de 409 | patch guard non appliqué | appliquer `kyc.controller.PATCHED.ts` |
| Login MFA échoue après déploiement chiffrement | — | impossible par conception : `SecretBox.open` fait passthrough des secrets legacy (SB-05) ; si ça arrive, vérifier `MFA_ENC_KEY` identique entre instances |
| `append-only: UPDATE interdit sur …` | tentative de mutation d'un journal | c'est le trigger R48 qui fait son travail — corriger l'appelant, jamais le trigger |
| Jeton OIDC rejeté « kid inconnu » | rotation IdP + cache | le vérificateur re-fetch une fois tout seul (JV-04) ; si persistant : `OIDC_JWKS_URI` erroné |
| SELECT rend 0 ligne partout après bascule T2 | `forTenant` non routé (GUC jamais posé) | vérifier que les requêtes passent par `forTenant` ; rollback = repasser au rôle propriétaire (T1) |
| CI verte mais CPSI rouge dans les logs | faux-vert du runner | appliquer le `sys.exit` d'une ligne ; la garde grep `### 18/18 suites vertes ###` est le filet |

## 5. Sauvegarde & preuve réglementaire

- Les journaux (`audit_log`, `kyc_question_history`, `screening_runs`, `screening_qualifications`,
  `domain_events`) sont **append-only sous trigger** : la sauvegarde est la seule voie de purge,
  jamais un DELETE.
- Réponse à l'inspecteur « cette inscription a-t-elle été confrontée à votre base ? » :
  `GET /v1/screening/runs` — périmètre, version de liste, seuil, pré-filtre, horodatage (R103),
  lisible sans reconstruction.

## 6. Activation du coffre Exoscale (R144→R147)

Le coffre (`CoffreService`) est **débranché par défaut** : sans port de stockage, tout dépôt est
refusé explicitement (R144 — « pas de dépôt fantôme »). L'adaptateur de production
`s3-storage.adapter.ts` (Exoscale SOS, S3-compatible, résidence suisse) est **exclu du typecheck**
(il dépend de `@aws-sdk/client-s3`, hors périmètre du corpus) ; il se câble à l'activation.

**Prérequis** :
1. Installer le SDK au déploiement : `pnpm --filter @olive/api add @aws-sdk/client-s3` et retirer
   l'exclusion `src/modules/coffre/s3-storage.adapter.ts` de `apps/api/tsconfig.json`.
2. Variables d'environnement (secrets) : `EXOSCALE_SOS_KEY`, `EXOSCALE_SOS_SECRET`,
   `EXOSCALE_SOS_BUCKET`. Sans elles, `exoscaleStoragePort()` **refuse au boot** (fail-fast R144).
3. Région : servie par le **registre R-Q** `storageRegion` (défaut `ch-gva-2`) — engagement de
   résidence contractuel, jamais en dur. Chiffrement par tenant : `storageChiffrement`.

**Câblage** (verbatim) :
```ts
new CoffreService(prisma, audit, { storage: exoscaleStoragePort() })
```

**Recette de bascule** (par tenant, jamais global) :
- (a) **Activer sur un tenant de test** : poser `storageRegion` (R-Q) + les env Exoscale, câbler le port.
- (b) **Dépôt / lecture / altération** : `ecrire(ctx, versionId, contenu)` (empreinte R111 vérifiée
  au dépôt) ; `lire` recalcule l'empreinte AVANT de servir ; **altérer volontairement l'objet au
  bucket** puis relire → **R145 refuse** (« intégrité du coffre en défaut — contenu NON servi »)
  et émet `coffre.integrite.alerte`. Le contenu altéré ne sort jamais.
- (c) **Réconciliation R147 à vide** : `reconcilier(ctx)` sur un tenant cohérent base↔coffre
  → **aucun** événement `coffre.reconciliation.orphelin` ni `…manquant`. Un écart d'inventaire est
  un FAIT D'AUDIT (orphelin → alerte ; manquant → CRITIQUE + tâche, une fois) — jamais de ménage
  automatique (R39/R147). La purge n'existe qu'en destruction certifiée R115 (l'empreinte survit).

## 7. Migrations versionnées (bascule lot 42, 21.07.2026)

Le dépôt fonctionnait en `prisma db push` (schéma poussé, aucun historique). **Depuis le lot 42,
le schéma est versionné par migrations Prisma** — `db push` ne sert plus qu'en sandbox jetable.

- **Baseline** : `prisma/migrations/0_baseline_lot42/migration.sql` fige l'état complet du schéma
  (48 tables/enums) à la bascule. Générée par `prisma migrate diff --from-empty
  --to-schema-datamodel prisma/schema.prisma --script`.
- **Toute évolution de schéma désormais** : `npm run prisma:migrate` (`prisma migrate dev -n <nom>`)
  — **jamais** `db push` hors sandbox. La migration est commitée avec le changement de `schema.prisma`.
- **Déploiement / CI** : `prisma migrate deploy` (remplace `db push` dans `ci.yml`), puis `prisma:post`
  (triggers R48 + RLS FORCE, hors périmètre migrations). Vérifié : `migrate reset`/`deploy` rejoue la
  baseline, e2e 6/6, recette RLS `clients` → 0 ligne sans GUC.
- **Base existante déjà poussée par `db push`** (sans `_prisma_migrations`) : marquer la baseline
  appliquée une fois — `prisma migrate resolve --applied 0_baseline_lot42` — puis basculer sur `deploy`.
